# candidate-profile

- **ProfileComponent** — public candidate profile (`/candidate/:profileId`)
- **ProfileTrainComponent** — profile editor and AI training (`/profile`)

## Running unit tests

Run `nx test candidate-profile` to execute the unit tests.
