{
  "name": "state-dashboard",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/states/dashboard/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:state", "type:state"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/states/dashboard"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
