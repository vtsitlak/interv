{
  "name": "state-interview",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/states/interview/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:state", "type:state"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/states/interview"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
