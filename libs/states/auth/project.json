{
  "name": "state-auth",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/states/auth/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:state", "type:state"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/states/auth"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
